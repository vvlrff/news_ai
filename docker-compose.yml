version: '3'
services:
  frontend:
    build:
      context: ./frontend
    ports:
      - 3000:3000
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
  
  backend:
    build:
      context: ./server
    ports:
      - 8000:8000
    volumes:
      - ./server:/app